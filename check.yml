---
- name: Check Demo
  hosts: localhost
  tasks:
      - name: Cd to the directory
        command: |
            echo "{{lookup('env','reponame')}}" | sudo install nginx
        register: output
      - name: Display
        debug:
          msg: "{{output.stdout}}"
        
